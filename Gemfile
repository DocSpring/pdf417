# frozen_string_literal: true

# A sample Gemfile
source 'https://rubygems.org'

group :test do
  gem 'shoulda', require: false
end

gemspec

# Dev dependencies
gem 'bundler', '~> 2.5'
gem 'minitest', '~> 5.0'
gem 'ostruct', '~> 0.6.0'
gem 'rake', '~> 13'
